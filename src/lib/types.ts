export interface Game {
    slug: string;
    title: string;
    description: string;
    coverUrl: string;
    previewVideoUrl: string;
    buildFolder: string;
    buildName: string;
    isPublished: boolean;
}
